<chapt>Tracing Kernel Oops's<label id="oops">
<P>
<sect>General Advice on Tracing Bugs Associated with the Kernel
<P>

<sect>Using ksymoops
<P>
The program <tt>ksymoops</tt> is in the <tt>scripts</tt> directory
of the kernel source files.  You can compile it with
<verb>
gcc -o ksymoops ksymoops.cc -lg++
</verb>

<P>
Execute it by running something like
<verb>
ksymoops System.map < oops
</verb>

where <tt>System.map</tt> is the map of the kernel which has
sent an oops message, and <tt>oops</tt> is a a file
containing the oops output.


