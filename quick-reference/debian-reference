#!/bin/sh
# POSIX compliant script to search browser
set -e # stop at first error
#set -x # debug output

if [ -r $BDOCUMENTSTEM/index.html ]; then
  /usr/bin/sensibl~browse $BDOCUMENTSTEM/index.html
fi
