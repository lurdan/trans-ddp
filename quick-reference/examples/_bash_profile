# ~/.bash_profile: executed by bash(1) for login shells.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# default environment is set in /etc/login.defs
mesg n		# no message (talkd)
#umask 002	# group share
umask 022	# traditional
#umask 077	# restrictive

# override PATH for admin-type user (comment out for normal user)
#PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/bin/X11:/usr/games

# set PATH so it includes user's private bin if it exists
if [ -d ~/bin ] ; then
    PATH=~/bin:"${PATH}"
fi

# do the same with MANPATH
#if [ -d ~/man ]; then
#    MANPATH=~/man:"${MANPATH}"
#fi

CDPATH=$HOME:/usr/share/doc:/usr/local

# include alias and function from .bashrc if it exists
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

export PATH CDPATH MANPATH

##### site specific environments
#export SFUSER=osamu
