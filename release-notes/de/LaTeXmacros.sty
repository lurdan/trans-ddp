% wait until German becomes the active language otherwise this would be
% related to English and ignored in German mode
\AtBeginDocument{
 % use \showhyphens{Debian LaTeX} to obtain default hyphenations in log file
 % (it's hard to find, search for first "Underfull \hbox")
 % do a 'debiandoc2latexdvi -k -l de release-notes.de.sgml' to create .log file
 \hyphenation{
   Viren-scannern Paketes
 }
}
