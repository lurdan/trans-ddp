%%
%% This is a wrapper for DB2LaTeX (http://db2latex.sf.net)
%% It main task is to ignore the problematic package option "hyperlink"
%%
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{db2latex}[2009/02/13 wrapper for db2latex.sty]

% The defined options
\DeclareOption{hyperlink}{} % does nothing!
\DeclareOption{nohyperlink}{} % this too :-)

% Default values
\ExecuteOptions{nohyperlink}

% If defined, process the options
\ProcessOptions\relax

% Now load the proper package without option [hyperlink].

% We specify also a path component to avoid a recursion
% Ugly: We assume that also the parent directory is in the TeX search path
% so that /usr/share/texmf/tex/latex/dblatex/contrib/db2latex/db2latex is
% found and not this file.
\usepackage{db2latex/db2latex}

