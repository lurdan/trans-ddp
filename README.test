Makefile:

Used by the autobuild system to build all the manuals.  Set PUBLISHDIR
in here to where you want the manuals installed.  Run 'make' to
install, 'make clean' to clean.


standard.makefile:

Template Makefile for use in the actual manual directories.  Modify to
suit.

