#!/bin/sh
# diff $1 $2 for po files
msgcat --no-location $1 -o $1.clean
msgcat --no-location $2 -o $2.clean
diff -u $1.clean $2.clean > ${2%%.*}.clean.diff
wdiff -d ${2%%.*}.clean.diff >${2%%.*}.clean.wdiff

